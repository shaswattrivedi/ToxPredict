--- src/pages/Home.jsx
+++ src/pages/Home.jsx
@@ -58,26 +58,10 @@
         {/* Subtle Chemical Overlay */}
         <div className="fixed inset-0 z-[-1] pointer-events-none opacity-40 bg-chemical-pattern transition-opacity duration-1000 transform-gpu" aria-hidden="true" />
         
         <div className="relative z-10 flex flex-col min-h-[100dvh] transform-gpu">
           <Header
             title="ToxPredict"
             tagline="Drug Toxicity Prediction with Explainability"
-            leftAction={(
-              <button
-                onClick={() => setShowComparison(!showComparison)}
-                className="text-xs border border-gray-600 text-gray-400 hover:text-white hover:border-gray-400 rounded px-3 py-1 transition-colors"
-              >
-                {showComparison ? 'Hide Comparison' : '⚖️ Compare Mode'}
-              </button>
-            )}
           />
-
-          {showComparison && (
-            <div className="pt-24">
-              <div className="max-w-7xl mx-auto px-4 mb-6">
-                <ComparisonMode />
-              </div>
-            </div>
-          )}
 
           <main className={`flex-1 flex flex-col w-full transition-all duration-500`}>
             {/* Landing viewport exactly preserved */}
